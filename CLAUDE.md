# Claude Instructions

Start with `./.claude/rules/director.md`.

Important repo rule:

- Cursor is canonical.
- Claude files mirror the Cursor instruction set for compatibility inside this repo.
- Keep `.claude` aligned with `.cursor` whenever instructions change.

# Codex Instructions

Start with `./.codex/rules/director.md`.

Important repo rule:

- Cursor is canonical.
- Codex files mirror the Cursor instruction set for compatibility inside this repo.
- Keep `.codex` aligned with `.cursor` whenever instructions change.

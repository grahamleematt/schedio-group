# Codex Instructions

Start with `./.codex/rules/director.md`.

If the file `./.claude/contributor` exists, this machine belongs to a
non-developer collaborator: read `./.codex/rules/contributor.md` FIRST and
follow it for the whole session.

Important repo rule:

- Cursor is canonical.
- Codex files mirror the Cursor instruction set for compatibility inside this repo.
- Keep `.codex` aligned with `.cursor` whenever instructions change.
